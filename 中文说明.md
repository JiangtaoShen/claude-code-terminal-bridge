# Claude Code Terminal Bridge

**基于文件 IPC 的终端桥接工具，让 Claude Code 通过 Windows CMD 窗口操控远程 SSH 会话。使用 pywinpty + pyte 实现实时屏幕捕获与命令注入。**

```
┌─────────────┐     command.txt      ┌──────────────────┐      PTY       ┌─────────────┐
│ Claude Code │  ──────────────────> │terminal_bridge.py│ ────────────>  │ SSH Session │
│  (任意项目)   │ <────────────────── │    (CMD 窗口)     │ <────────────  │  (远程服务器) │
└─────────────┘     output.txt       └──────────────────┘                └─────────────┘
```

## 环境要求

- **Windows 10+**（使用 ConPTY）
- **Python 3.8+**

## 快速开始

### 1. 克隆 & 安装

```bash
git clone https://github.com/JiangtaoShen/claude-code-terminal-bridge.git
cd claude-code-terminal-bridge
pip install -r requirements.txt
```

### 2. 启动桥接

打开一个 **CMD 窗口**，运行：

```bash
py terminal_bridge.py
```

你会看到顶部状态栏和 CMD 提示符。手动 SSH 登录你的服务器：

```bash
ssh user@your-server.com
# 手动输入密码
```

### 3. 在任意 Claude Code 项目中使用

在**任意** Claude Code 项目中，告诉 Claude：

> "我已经启动了 terminal bridge 并登录了 SSH，IPC 文件在 `~/.terminal-bridge/`。"

Claude Code 会：
- **读取** `~/.terminal-bridge/output.txt` 查看终端屏幕
- **写入** `~/.terminal-bridge/command.txt` 发送命令
- **读取** `~/.terminal-bridge/result.txt` 获取命令完整输出
- **读取** `~/.terminal-bridge/status.json` 查看执行状态

## 命令协议

向 `command.txt` 写入命令，支持以下格式：

| 前缀 | 说明 | 示例 |
|------|------|------|
| *（无）* | 发送文本 + 回车 | `ls -la` |
| `EXEC:id:cmd` | 带请求 ID 的完整输出捕获 | `EXEC:req_001:find / -name "*.log"` |
| `EXEC:CLEAN:id:cmd` | 带 ANSI 清洗的 EXEC | `EXEC:CLEAN:req_002:cat log.txt` |
| `QUERY:id:cmd` | 短输出快速查询 | `QUERY:q_001:nvidia-smi` |
| `BATCH:id` | 批量顺序执行多条命令 | 见下文 |
| `SCROLLBACK:N` | 导出滚动历史最后 N 行 | `SCROLLBACK:200` |
| `LAST:N` | 屏幕最后 N 行非空内容（不发送命令） | `LAST:5` |
| `RAW:` | 发送原始字节 | `RAW:\x03`（Ctrl+C） |
| `KEY:` | 发送特殊按键 | `KEY:CTRL+C`、`KEY:UP`、`KEY:TAB` |

### 请求 ID 关联机制

每个 `EXEC:` 和 `QUERY:` 命令都包含唯一的请求 ID。result.txt 中的结果也包含此 ID，Claude 可以验证读到的是正确的结果：

```
[req_id: req_001]
[status: done]
[exit_code: 0]
[timestamp: 2026-04-03T02:05:11]
[duration: 0.3s]
[cmd: nvidia-smi]
...实际输出...
```

### status.json 中的执行状态

`status.json` 包含 `exec_queue` 字段，显示当前执行和最近完成的命令：

```json
{
  "pty_alive": true,
  "exec_queue": {
    "current": {
      "req_id": "req_003",
      "cmd": "ps aux | grep train",
      "status": "running",
      "started_at": "2026-04-03T02:05:10"
    },
    "last_completed": {
      "req_id": "req_002",
      "status": "done",
      "exit_code": 0,
      "duration_s": 1.2,
      "completed_at": "2026-04-03T02:05:09"
    }
  }
}
```

### 批量执行

一次请求执行多条命令：

```
BATCH:batch_001
EXEC:nvidia-smi --query-gpu=index,utilization.gpu --format=csv,noheader
EXEC:ps aux | grep "python.*train" | grep -v grep
EXEC:ls -lt ~/output/*pde5* 2>/dev/null
END_BATCH
```

结果在 result.txt 中按命令分段显示，每条命令有独立的退出码。

### 何时使用哪种命令

- **`EXEC:id:cmd`**：标准命令执行，完整输出捕获。使用后台 shell 执行，避免终端噪音干扰。
- **`EXEC:CLEAN:id:cmd`**：同 EXEC 但自动清除 ANSI 转义序列。当输出含 tqdm 进度条、彩色文本等控制字符时使用。
- **`QUERY:id:cmd`**：短输出快速查询（<20 行），直接从屏幕变化捕获，无需文件重定向。15 秒超时。
- **`BATCH:id`**：多条命令顺序执行，减少往返开销。
- **`SCROLLBACK:N`**：屏幕被进度条覆盖时，检索滚动历史。
- **`LAST:N`**：轻量级状态检查——只读屏幕，不发送命令。消耗极少 token。

### 支持的按键

`CTRL+C`, `CTRL+D`, `CTRL+Z`, `CTRL+L`, `CTRL+A`, `CTRL+E`, `CTRL+K`, `CTRL+U`, `CTRL+W`, `CTRL+R`, `ENTER`, `TAB`, `BACKSPACE`, `ESCAPE`, `UP`, `DOWN`, `LEFT`, `RIGHT`, `HOME`, `END`, `DELETE`, `PAGEUP`, `PAGEDOWN`

## 文件说明

所有 IPC 文件默认存放在 `~/.terminal-bridge/` 下：

| 文件 | 方向 | 说明 |
|------|------|------|
| `output.txt` | Bridge -> Claude | 当前终端屏幕内容（自动去除尾部空行） |
| `command.txt` | Claude -> Bridge | 待执行的命令 |
| `result.txt` | Bridge -> Claude | `EXEC:` / `QUERY:` / `BATCH:` / `LAST:N` / `SCROLLBACK:N` 的输出 |
| `status.json` | Bridge -> Claude | 桥接状态（PID、存活状态、时间戳、执行队列） |

### Token 节省设计

- **`output.txt`** 自动去除尾部空行，只保留有实际内容的行。
- **`LAST:N`** 只返回 N 行内容，比读取整个屏幕省大量 token。
- **`QUERY:`** 短输出直接从屏幕变化捕获，比完整 EXEC 流程更快。
- **`BATCH:`** 一次请求执行多条命令，减少往返开销。
- **原子写入**：所有结果文件使用 `os.replace()` 原子写入，防止 Claude 读到写了一半的文件。

## 命令行参数

```
py terminal_bridge.py [OPTIONS]

参数:
  --dir DIR        IPC 文件目录（默认: ~/.terminal-bridge/）
  --shell SHELL    启动的 Shell（默认: cmd.exe）
  --cols N         终端列数（默认: 自动检测）
  --rows N         终端行数（默认: 自动检测）
  --output PATH    自定义 output.txt 路径
  --command PATH   自定义 command.txt 路径
  --status PATH    自定义 status.json 路径
```

## 常见问题

**Q: 可以在任意 Claude Code 项目中使用吗？**

可以！IPC 文件存放在固定全局路径（`~/.terminal-bridge/`），任何项目都能访问。

**Q: 怎么输入密码？**

直接在 Bridge CMD 窗口中手动输入。不要通过 `command.txt` 发送密码。

**Q: 命令输出太长怎么办？**

使用 `EXEC:` 前缀，输出会重定向到远程临时文件再读回。超大输出（>50KB）建议手动重定向。

**Q: 输出里有进度条 / ANSI 控制字符怎么办？**

使用 `EXEC:CLEAN:` 自动清除 ANSI 转义序列和回车符。解决 tqdm 进度条、彩色文本和 grep 返回 "Binary file matches" 的问题。

**Q: 怎么节省 token 查看任务进度？**

使用 `LAST:3` 或读取 `status.json` 的 `exec_queue.current.status`。

## 常见陷阱

**1. Bridge 必须由用户在可见 CMD 窗口中手动启动**

Claude Code 无法弹出可见窗口。用户必须自己打开 CMD 并运行 `py terminal_bridge.py`。

**2. SSH 必须在 Bridge 窗口内操作**

Bridge 只捕获它自己的 PTY。务必在有 `BRIDGE | ...` 状态栏的窗口中操作。

**3. 每次启动都是全新会话**

重启 Bridge 后旧 SSH 连接断开，必须重新登录。

**4. 务必验证 result.txt 中的请求 ID**

通过请求 ID 机制，始终检查 `[req_id:]` 是否匹配你的请求，防止读到旧命令的结果。

**5. 含 ANSI 码的日志用 EXEC:CLEAN**

远程日志中的 tqdm 进度条含 ANSI 转义序列，会导致 grep 等工具异常。CLEAN 变体自动清理。

**6. 多命令查询用 BATCH**

不必发送 3 次 EXEC 并在中间 sleep，一条 BATCH 命令即可完成，减少总延迟。

## 工作原理

1. **pywinpty** 创建 Windows 伪终端（ConPTY），运行 `cmd.exe`
2. **pyte** 将原始 ANSI 转义序列渲染为可读纯文本
3. 后台线程每 0.5 秒将屏幕内容写入 `output.txt`（自动去除尾部空行）
4. 另一个线程每 0.2 秒检查 `command.txt`，将命令转发到 PTY
5. `EXEC:` 在后台 shell 中执行（独立于终端流），结果写入临时文件后用请求专属分隔符读回
6. `QUERY:` 短输出直接从屏幕变化捕获，低延迟
7. `BATCH:` 多命令顺序执行，结构化分段结果
8. `SCROLLBACK:` 导出 pyte 历史缓冲区，找回被进度条推出屏幕的输出
9. 所有文件写入使用 `os.replace()` 保证原子性
10. `status.json` 包含 `exec_queue` 执行队列状态，支持确定性轮询
11. Bridge 窗口本身是透明终端，可直接在里面打字
