# Claude Code Terminal Bridge

**基于文件 IPC 的终端桥接工具，让 Claude Code 通过 Windows CMD 窗口操控远程 SSH 会话。使用 pywinpty + pyte 实现实时屏幕捕获与命令注入。**

```
┌─────────────┐                        ┌──────────────────┐      PTY       ┌─────────────┐
│ Claude Code │  ── bridge_run.py ──>  │terminal_bridge.py│ ────────────>  │ SSH Session │
│  (任意项目)   │ <── result.txt ──────  │    (CMD 窗口)     │ <────────────  │  (远程服务器) │
└─────────────┘                        └──────────────────┘                └─────────────┘
```

## 环境要求

- **Windows 10+**（使用 ConPTY）
- **Python 3.8+**

## 快速开始

### 1. 克隆 & 安装

```bash
git clone https://github.com/JiangtaoShen/claude-code-terminal-bridge.git
cd claude-code-terminal-bridge
pip install -r requirements.txt
```

### 2. 启动桥接

打开一个 **CMD 窗口**，运行：

```bash
py terminal_bridge.py
```

你会看到 `[Bridge v2.0.0]` 启动信息和顶部状态栏。手动 SSH 登录服务器：

```bash
ssh user@your-server.com
# 手动输入密码
```

### 3. 执行远程命令

#### 使用 `bridge_run.py`（推荐）

一条命令完成写入、轮询、读取：

```bash
# 单条命令
py bridge_run.py "ls ~/data"

# 纯输出（跳过元数据）
py bridge_run.py --raw "nvidia-smi"

# 清除 ANSI 转义码（日志含颜色/进度条时使用）
py bridge_run.py --clean "tail -50 ~/train.log"

# 自定义超时
py bridge_run.py --timeout 30 "quick_check"

# 检查 SSH 连接
py bridge_run.py --last 3

# 读取滚动历史
py bridge_run.py --scroll 200

# 查看桥接状态
py bridge_run.py --status
```

退出码与远程命令一致。超时返回 124。

#### 使用原始 IPC（备用）

直接写入 `~/.terminal-bridge/command.txt`：

```bash
echo "EXEC:req_001:your_command" > ~/.terminal-bridge/command.txt
# 轮询 status.json 直到完成，然后读取 result.txt
```

## 架构

### v2.0.0 更新

- **PTY 直通执行**：所有命令（EXEC、QUERY、BATCH）通过 PTY 直接发送，使用起止标记提取输出。透明支持 SSH 会话。
- **`bridge_run.py` 命令行助手**：一次调用完成命令执行与结果获取，替代手动 `echo > command.txt && sleep N && cat result.txt` 模式。
- **单实例锁**：防止多个桥接进程竞争 `command.txt`。
- **`status.json` 版本字段**：便于确认运行的代码版本。

### 工作原理

1. **pywinpty** 创建 Windows 伪终端（ConPTY），运行 `cmd.exe`
2. **pyte** 将原始 ANSI 转义序列渲染为可读纯文本
3. 后台线程每 0.5 秒将屏幕内容写入 `output.txt`
4. 另一个线程每 0.2 秒检查 `command.txt`，将命令转发到 PTY
5. `EXEC:` 通过 PTY 发送 `echo <起始标记>; cmd; echo <结束标记> $?`，从 pyte 屏幕缓冲区提取标记之间的输出
6. `BATCH:` 使用相同的标记机制顺序执行多条命令
7. 所有文件写入使用 `os.replace()` 保证原子性
8. `bridge_run.py` 写入 `command.txt`，轮询 `status.json` 等待完成，然后读取 `result.txt`

## 命令协议

| 前缀 | 说明 | 示例 |
|------|------|------|
| *（无）* | 发送文本 + 回车 | `ls -la` |
| `EXEC:id:cmd` | 带请求 ID 的输出捕获 | `EXEC:req_001:find / -name "*.log"` |
| `EXEC:CLEAN:id:cmd` | 带 ANSI 清洗的 EXEC | `EXEC:CLEAN:req_002:cat log.txt` |
| `QUERY:id:cmd` | 同 EXEC，30 秒超时 | `QUERY:q_001:pwd` |
| `BATCH:id` | 批量顺序执行 | 见下文 |
| `SCROLLBACK:N` | 导出滚动历史 | `SCROLLBACK:200` |
| `LAST:N` | 屏幕最后 N 行（不发命令） | `LAST:5` |
| `RAW:` | 发送原始字节 | `RAW:\x03` |
| `KEY:` | 发送特殊按键 | `KEY:CTRL+C` |

### 批量执行

```
BATCH:batch_001
EXEC:nvidia-smi --query-gpu=index,utilization.gpu --format=csv,noheader
EXEC:ps aux | grep "python.*train" | grep -v grep
EXEC:ls -lt ~/output/ | head -5
END_BATCH
```

## 文件说明

| 文件 | 说明 |
|------|------|
| `terminal_bridge.py` | 桥接服务端，运行在 CMD 窗口中 |
| `bridge_run.py` | Claude Code 命令行助手，一次调用完成执行 |
| `~/.terminal-bridge/command.txt` | Claude -> Bridge：待执行命令 |
| `~/.terminal-bridge/result.txt` | Bridge -> Claude：命令输出 |
| `~/.terminal-bridge/status.json` | Bridge -> Claude：状态、执行队列、版本 |
| `~/.terminal-bridge/output.txt` | Bridge -> Claude：终端屏幕原始内容 |
| `~/.terminal-bridge/bridge.lock` | 单实例锁文件 |

## 命令行参数

### terminal_bridge.py

```
py terminal_bridge.py [OPTIONS]

  --dir DIR        IPC 文件目录（默认: ~/.terminal-bridge/）
  --shell SHELL    启动的 Shell（默认: cmd.exe）
  --cols N         终端列数（默认: 自动检测）
  --rows N         终端行数（默认: 自动检测）
```

### bridge_run.py

```
py bridge_run.py [OPTIONS] "command"

  --raw            纯输出（跳过元数据头）
  --clean          清除 ANSI 转义码
  --timeout N      超时秒数（默认: 130）
  --last N         读取屏幕最后 N 行
  --scroll N       读取滚动历史 N 行
  --status         显示桥接状态
  --id ID          自定义请求 ID
```

## 常见问题

**Q: 可以在任意项目中使用吗？**
可以。IPC 文件在固定全局路径 `~/.terminal-bridge/`。

**Q: 怎么输入密码？**
在 Bridge 窗口中手动输入，不要通过 `command.txt` 发送。

**Q: 提示"另一个桥接正在运行"？**
v2.0.0 有单实例保护。关闭旧窗口或 `taskkill /F /PID <pid>`。

**Q: 输出有进度条/ANSI 码？**
使用 `bridge_run.py --clean` 自动清除。

**Q: 怎么低成本查看任务进度？**
`bridge_run.py --last 3` 只读几行屏幕内容。

## 常见陷阱

1. **Bridge 必须由用户在可见 CMD 窗口中启动。**
2. **SSH 必须在 Bridge 窗口内操作。**
3. **每次启动都是全新会话，**重启后需重新 SSH。
4. **务必验证请求 ID。**`bridge_run.py` 自动完成验证。
5. **多命令查询用 BATCH，**减少延迟。
