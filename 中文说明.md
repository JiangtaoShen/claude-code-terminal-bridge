# Claude Code Terminal Bridge

**基于文件 IPC 的终端桥接工具，让 Claude Code 通过 Windows CMD 窗口操控远程 SSH 会话。使用 pywinpty + pyte 实现实时屏幕捕获与命令注入。**

```
┌─────────────┐     command.txt      ┌──────────────────┐      PTY       ┌─────────────┐
│ Claude Code │  ──────────────────> │terminal_bridge.py│ ────────────>  │ SSH Session │
│  (任意项目)   │ <────────────────── │    (CMD 窗口)     │ <────────────  │  (远程服务器) │
└─────────────┘     output.txt       └──────────────────┘                └─────────────┘
```

## 环境要求

- **Windows 10+**（使用 ConPTY）
- **Python 3.8+**

## 快速开始

### 1. 克隆 & 安装

```bash
git clone https://github.com/JiangtaoShen/claude-code-terminal-bridge.git
cd claude-code-terminal-bridge
pip install -r requirements.txt
```

### 2. 启动桥接

打开一个 **CMD 窗口**，运行：

```bash
py terminal_bridge.py
```

你会看到顶部状态栏和 CMD 提示符。手动 SSH 登录你的服务器：

```bash
ssh user@your-server.com
# 手动输入密码
```

### 3. 在任意 Claude Code 项目中使用

在**任意** Claude Code 项目中，告诉 Claude：

> "我已经启动了 terminal bridge 并登录了 SSH，IPC 文件在 `~/.terminal-bridge/`。"

Claude Code 会：
- **读取** `~/.terminal-bridge/output.txt` 查看终端屏幕
- **写入** `~/.terminal-bridge/command.txt` 发送命令
- **读取** `~/.terminal-bridge/result.txt` 获取命令完整输出

## 命令协议

向 `command.txt` 写入命令，支持五种格式：

| 前缀 | 说明 | 示例 |
|------|------|------|
| *（无）* | 发送文本 + 回车 | `ls -la` |
| `EXEC:` | 完整输出捕获到 `result.txt` | `EXEC:find / -name "*.log"` |
| `LAST:N` | 将屏幕最后 N 行非空内容写入 `result.txt`（不发送命令） | `LAST:5` |
| `RAW:` | 发送原始字节 | `RAW:\x03`（Ctrl+C） |
| `KEY:` | 发送特殊按键 | `KEY:CTRL+C`、`KEY:UP`、`KEY:TAB` |

### 何时使用哪种命令

- **普通命令**（`ls -la`）：简短命令，之后通过 `output.txt` 读取屏幕内容。输出受终端可见区域限制。
- **`EXEC:`**：长时间运行或输出量大的命令。结果通过唯一分隔符可靠捕获到 `result.txt`，不受屏幕大小限制。
- **`LAST:N`**：轻量级状态查询——只读取当前屏幕最后 N 行非空内容，不向终端发送任何命令。适合查看进度条、日志尾部、提示符等。消耗极少 token。

### 支持的按键

`CTRL+C`, `CTRL+D`, `CTRL+Z`, `CTRL+L`, `CTRL+A`, `CTRL+E`, `CTRL+K`, `CTRL+U`, `CTRL+W`, `CTRL+R`, `ENTER`, `TAB`, `BACKSPACE`, `ESCAPE`, `UP`, `DOWN`, `LEFT`, `RIGHT`, `HOME`, `END`, `DELETE`, `PAGEUP`, `PAGEDOWN`

## 文件说明

所有 IPC 文件默认存放在 `~/.terminal-bridge/` 下：

| 文件 | 方向 | 说明 |
|------|------|------|
| `output.txt` | Bridge -> Claude | 当前终端屏幕内容（自动去除尾部空行） |
| `command.txt` | Claude -> Bridge | 待执行的命令 |
| `result.txt` | Bridge -> Claude | `EXEC:` 或 `LAST:N` 命令的输出 |
| `status.json` | Bridge -> Claude | 桥接状态信息（PID、存活状态、时间戳） |

### Token 节省设计

- **`output.txt`** 自动去除尾部空行，只保留有实际内容的行，而非完整的 40 行屏幕。
- **`LAST:N`** 只返回 N 行内容（如 `LAST:3` 快速检查进度），比读取整个屏幕省大量 token。
- **`EXEC:`** 使用唯一分隔符（`__BRIDGE_BEGIN_RESULT__` / `__BRIDGE_END_RESULT__`）可靠提取输出，避免旧方案中屏幕历史解析导致的结果混入终端噪音。

## 命令行参数

```
py terminal_bridge.py [OPTIONS]

参数:
  --dir DIR        IPC 文件目录（默认: ~/.terminal-bridge/）
  --shell SHELL    启动的 Shell（默认: cmd.exe）
  --cols N         终端列数（默认: 自动检测）
  --rows N         终端行数（默认: 自动检测）
  --output PATH    自定义 output.txt 路径
  --command PATH   自定义 command.txt 路径
  --status PATH    自定义 status.json 路径
```

### 示例

```bash
# 默认: IPC 文件在 ~/.terminal-bridge/
py terminal_bridge.py

# 自定义目录
py terminal_bridge.py --dir D:\my_project\bridge

# 使用 PowerShell 代替 CMD
py terminal_bridge.py --shell powershell.exe
```

## 常见问题

**Q: 可以在任意 Claude Code 项目中使用吗？**

可以！IPC 文件存放在固定全局路径（`~/.terminal-bridge/`），任何项目的 Claude Code 都能访问。

**Q: 怎么输入密码？**

直接在 Bridge CMD 窗口中手动输入。不要通过 `command.txt` 发送密码。

**Q: 命令输出太长怎么办？**

使用 `EXEC:` 前缀，输出会重定向到远程临时文件，再通过唯一分隔符可靠读回。超大输出（>50KB）建议手动重定向：`some_command > /tmp/result.log`。

**Q: Claude 使用时我还能在 Bridge 窗口打字吗？**

可以，手动输入和 Claude 的命令可以同时使用。

**Q: 怎么节省 token 查看任务进度？**

使用 `LAST:3` 或 `LAST:5`——只读取屏幕最后几行非空内容，不发送任何命令。非常适合查看进度条和日志尾部。

## 常见陷阱

**1. Bridge 必须由用户在可见 CMD 窗口中手动启动**

Claude Code 无法通过 `start cmd /k ...` 或后台 Bash 弹出可见窗口——Bridge 会在 Claude 的不可见子进程中运行。**用户必须自己打开 CMD 并运行 `py terminal_bridge.py`。**

**2. SSH 必须在 Bridge 窗口内操作**

Bridge 只捕获它自己的 PTY。如果用户在其他终端 SSH 登录，Bridge 仍然显示本地 CMD 提示符。务必在顶部有 `BRIDGE | ...` 状态栏的窗口中操作 SSH。

**3. 每次启动 Bridge 都是全新会话**

重启 Bridge 后旧的 SSH 连接会断开，必须重新登录。旧的 `result.txt` / `output.txt` 可能残留上次会话的内容——重新连接后务必用 `LAST:3` 验证当前状态。

**4. 不要用 `output.txt` 验证命令结果**

`output.txt` 捕获的是整个可见屏幕，可能包含旧命令历史、SSH 登录 banner 等噪音。获取命令结果应始终使用 `EXEC:`（干净结果写入 `result.txt`）或 `LAST:N`（仅最后 N 行）。`output.txt` 仅用于调试连接状态。

**5. 状态检查优先用 `LAST:N` 而非 `output.txt`**

读取 `output.txt` 返回整个屏幕（~20-40 行）。`LAST:3` 只返回 3 行到 `result.txt`。查看进度时，`LAST:N` 节省大量 token。

## 工作原理

1. **pywinpty** 创建 Windows 伪终端（ConPTY），运行 `cmd.exe`
2. **pyte** 将原始 ANSI 转义序列渲染为可读纯文本
3. 后台线程每 0.5 秒将渲染后的屏幕内容写入 `output.txt`（自动去除尾部空行）
4. 另一个线程每 0.2 秒检查 `command.txt`，将命令转发到 PTY
5. `EXEC:` 命令将输出重定向到临时文件，再通过唯一分隔符可靠提取
6. `LAST:N` 直接读取 pyte 屏幕缓冲区，不发送任何命令——零延迟，极少 token
7. Bridge 窗口本身就是一个透明终端——你可以直接在里面打字操作
